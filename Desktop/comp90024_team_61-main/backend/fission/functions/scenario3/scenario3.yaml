apiVersion: apps/v1
kind: Deployment
metadata:
  name: scenario3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scenario3
  template:
    metadata:
      labels:
        app: scenario3
    spec:
      containers:
      - name: scenario3
        image: ache8/scenario3:latest
        ports:
        - containerPort: 5000
        env:
        - name: ES_USERNAME
          valueFrom:
            secretKeyRef:
              name: es-credentials
              key: username
        - name: ES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: es-credentials
              key: password
---
apiVersion: v1
kind: Service
metadata:
  name: scenario3
spec:
  selector:
    app: scenario3
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP
